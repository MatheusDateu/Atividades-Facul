Program ferraduras ;
    var cavalos,numfer:integer;
    Begin
        write('Informe a quantidade de cavalos= ');
        readln(cavalos);
        numfer:=(cavalos*4);
        write('A quantidade de ferraduras é= ',numfer);
    End.
